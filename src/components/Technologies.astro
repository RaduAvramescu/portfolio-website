---
import TechnologyItem from './TechnologyItem.astro';
import type { Technology } from '../utils/technologies';

export interface Props {
  technologies: Technology[];
  width?: number;
}

const { technologies, width = 40 } = Astro.props;
---

{technologies.map(tech => <TechnologyItem techName={tech} width={width} />)}
